# /etc/shadowdns/env
# chmod 600 this file

SD_API_TOKEN=change-me-to-a-long-random-token
# SD_TELEGRAM_TOKEN=
# SD_TELEGRAM_CHAT=

# Optional overrides if you prefer argv-free config:
# (token is also accepted via --token; env is preferred for systemd)
